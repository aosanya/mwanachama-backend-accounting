# mwanachama-backend-accounting — completed tasks

| Task | Title | Completed | Notes |
|------|-------|-----------|-------|
| W10 | Close open questions 1/2/4 (W7/W6/W5): member account type → `income`; ledger access control → an org-wide, treasury-scoped `CapViewLedger`/`CapPostLedger` pair; the statement-import suspense-account shape → one `AccountSuspense` per basket | 2026-09-06 | User-decided, not engineering-derived (see [2. design/README.md](../2.%20design/README.md)'s "Decisions closed 2026-09-06/07"). Built and tested in the pre-split shape: `AccountSuspense`, `DocContributionUnmatched`/`DocContributionMatched`, and `accountTypeFor`'s Kind→Type pairing enforced in `Account.Validate()` — all superseded one commit later by W11's generic-core split, where the pairing enforcement and the three account kinds move out of the root package. W6's capability decision has no code here — it's built in `mwanachama-backend-api-gateway`'s `internal/api/http/capability.go` when W9 lands, this repo has no HTTP layer. `go build`/`go vet`/`go test ./...` (memory backend) clean. |
| W11 | Split the ledger into a domain-agnostic core (root `accounting` package) + a `contribution/` subpackage carrying every Mwanachama-specific decision from W10 | 2026-09-07 | Prompted by being asked to make this pluggable for non-political domains (a school's fee ledger, a token ledger) — `AccountMember`/`MemberID` (and, once spotted, `AccountBasket`/`PaybillID`) baked "member"/"paybill" into the root package's closed vocabulary exactly the way `mwanachama-backend-assetmanager`'s `CLAUDE.md` explicitly keeps "chapter" *out* of `Location.Kind`. Collapsed the old `PartyID`/`PaybillID` duality into one generic `HolderID` field (every account now keyed on `(Kind, HolderID)`, no per-Kind field switch); removed the closed `AccountKind`/`DocumentKind` vocabularies and the Kind→Type pairing enforcement from `Account.Validate()` — `AccountType` (the five standard accounting types) is the only vocabulary the core still closes, since that's double-entry theory, not a domain decision. Moved `AccountExternal`/`AccountBasket`/`AccountSuspense`, `AccountTypeFor`, `OpenAccount`, and the four `DocumentKind` constants into new `contribution/contribution.go`. Free simplification this forced: `PostgresRepository`'s entity-graph mapping collapsed from three entity types (one per old Kind, since `TypeDefinition.UniqueKey` needed a different field per type) to one `account` type with a composite `(account_kind, holder_id)` unique key — `schema.go`, `postgres.go`, `memory.go` all got smaller, not larger, from generalizing. Core's own `conformance_test.go`/`ledger_test.go` rewritten to use arbitrary test-data Kind/DocumentKind values instead of contribution vocabulary; the contribution-specific suspense-flow test (unmatched → matched nets to zero) moved to `contribution/contribution_test.go`, run against the same `MemoryRepository`. `go build`/`go vet`/`go test ./...` clean on both the root package and `contribution/`. **Superseded one entry later by W13** — putting even a thin domain-vocabulary subpackage inside this repo was still the wrong address. |
| W14 | Add a repo-local `routes/` package (matching `mwanachama-backend-git`/`taskmanager`/`actor`/`agency`'s own shape) exposing this repo's generic `Account`/`Entry` CRUD directly | 2026-09-15 | [routes/doc.go](../../routes/doc.go), [routes.go](../../routes/routes.go), [wire.go](../../routes/wire.go), [errors.go](../../routes/errors.go), [account.go](../../routes/account.go) (`AccountRoutes`: `OpenAccount`/`GetAccount`/`ListAccounts`/`CloseAccount`/`GetAccountBalance`), [entry.go](../../routes/entry.go) (`EntryRoutes`: `PostEntry`/`ListEntries`) — 7 routes total, mirroring `taskmanager`'s `Route`/`Pattern`/`Routes()` shape and `actor`'s `<domain>StatusFor`/`write<Domain>Err` error-mapping shape byte-for-byte. Stays vocabulary-neutral per CLAUDE.md — no member/paybill/contribution concept anywhere, `W9`'s future `accountingadapter` still owns that translation. Necessary prerequisite found and fixed along the way: `Account`/`Entry` in `ledger.go` carried **no JSON struct tags at all**, so `encoding/json` would have emitted Go's default PascalCase field names (`ID`, `HolderID`, `FromAccountID`, ...) instead of the snake_case every sibling route package's wire.go comment promises ("consistent with every other route in this package") — added the standard snake_case tags (`id`, `kind`, `type`, `holder_id`, `opened_at`, `closed_at`, `posted_at`, `occurred_at`, `from_account_id`, `to_account_id`, `amount`, `document_kind`, `document_id`, `actor_id`, `detail`, `reverses_entry_id`), a minimal in-scope fix since the routes package couldn't have a correct wire contract without it. Also found and fixed, unrelated to this row but blocking any build/test at all: `go.mod`'s `toolchain go1.24.7` line predated `mwanachama-backend-shared` bumping its own `go.mod` to `go 1.25.0` — pinned the whole repo unbuildable under plain `go build`/`GOTOOLCHAIN=auto` resolution (a pre-existing break, not introduced by this row); fixed with `GOTOOLCHAIN=go1.25.0 go mod tidy`, which also picked up a handful of already-stale indirect dependency bumps (`pgx` v5.7.5→v5.10.0, dropped unused `lib/pq`/`golang.org/x/crypto`). Verified with `go build ./... && go vet ./... && go test ./...` (memory backend) — clean, including a new end-to-end test (`TestEndToEnd_OpenPostBalanceClose`) that drives a real `net/http.ServeMux` built from `routes.Routes` through open→post→balance→list-entries→close-refused-on-nonzero-balance→404-on-unknown-account, not just direct handler calls. |
| W13 | Remove `contribution/` from this repo entirely — even a thin domain-vocabulary subpackage inside a generic sibling repeats the exact coupling `mwanachama-backend-actor` avoids for "chapter" and `mwanachama-backend-forms` avoids for "survey" | 2026-09-07 | User correction: this repo should be a generic sibling like `actor`/`forms`/`assetmanager`, with domain mapping configured in the gateway, the same way `actor`'s `Group` becomes "chapter" via the gateway (not a subpackage inside `actor`) and `mwanachama-backend-forms`'s generic `FormManager` gets its survey vocabulary entirely from the gateway's `internal/store/formsadapter`, not from anything inside `forms` itself. `contribution/contribution.go`/`contribution_test.go` deleted outright (`rm -rf`); nothing in the root package referenced them, so no other file changed. The vocabulary itself (`AccountExternal`/`AccountBasket`/`AccountSuspense`, `AccountTypeFor`, the four `DocumentKind` constants, the suspense-flow test) is unchanged in substance and belongs in `mwanachama-backend-api-gateway`'s own `internal/store/accountingadapter` (mirroring `formsadapter`'s shape) once DEV-1674 lands — not written there yet, since that repo's `go.mod`/chokepoint files are under active concurrent edit from other sessions as of 2026-09-07 and adding the accounting dependency means touching `go.mod`. `documentation/2. design/README.md` and `CLAUDE.md` rewritten to point at that future address instead of the deleted subpackage. `go build`/`go vet`/`go test ./...` clean on the root package (now the only package in this repo again). |
| W1 | Bootstrap repo: `go.mod`, `.gitignore`, `Makefile`, four-phase `documentation/` skeleton, `todo.md`/`todo_done.md` | 2026-09-03 | New repo (`mwanachama-backend-accounting`, following the `mwanachama-backend-<domain>` naming convention shared by `taskmanager`/`comm`/`git`). Depends on [mwanachama-backend-shared](../../../mwanachama-backend-shared) for its entity-graph store (wired via `go.mod` local `replace`, same as `taskmanager`). Decisions recorded in [DSN-1698](../../../developer/documentation/2.%20design/todo.md) and [documentation/2. design/README.md](../2.%20design/README.md). |
| W2 | `Account`/`Entry` domain types + `Validate()`, mirroring `merchandise.Account`/`merchandise.Entry`'s shape: append-only, corrected only by a new entry with `ReversesEntryID`, no stored balance | 2026-09-03 | [ledger.go](../../ledger.go), [vocabulary.go](../../vocabulary.go) (`AccountKind`: `member`/`basket`; `AccountType`: the five standard types, open question 1 unresolved; `DocumentKind`: `contribution`/`correction`, more to follow with W5), [errors.go](../../errors.go). Every entry names both accounts — unlike the merchandise ledger there is no "evidence" one-sided kind in scope yet, so the zero-sum invariant is structural: an `Amount` always moves from exactly one account to exactly one other. |
| W3 | `LedgerRepository` interface + `MemoryRepository`, an in-process reference implementation (lazy `OpenAccount`, `Post`, `Balance` as a fold, `ListEntries`, `CloseAccount` refusing a non-zero balance) | 2026-09-03 | [ledger_repository.go](../../ledger_repository.go), [memory.go](../../memory.go) — not yet backed by `mwanachama-backend-shared`'s entity-graph at this point (that's W4); `MemoryRepository` is a standalone fake, the same role `taskmanager`'s `fakeDataManager` played before its own Postgres wiring. |
| W4 | Postgres schema/migration for `member_account`/`basket_account`/`entry` via `mwanachama-backend-shared`'s `schema` types + entity-graph `DataManager` | 2026-09-03 | [schema.go](../../schema.go) (`DefaultAccountingSchema()` — three types; `Account` is split into two entity-graph types, `member_account` keyed on `member_id` and `basket_account` keyed on `paybill_id`, because `TypeDefinition.UniqueKey` is one fixed field list per type and the two kinds key on different fields; `entry` is `Immutable: true` so `UpdateEntity` is refused by the DataManager itself, not only by convention), [postgres.go](../../postgres.go) (`PostgresRepository`, one per `AgencyID`; `FromAccountID`/`ToAccountID` are plain string properties on `entry`, not entity-graph relationships — a typed edge would need a `RelationshipDefinition` per (source, target) type pair for a property nothing here needs to traverse the graph for). `schema_test.go` — `entitygraph.ValidateSchema` sanity check, mirroring `taskmanager`'s. |
| W8 | Unit tests: one shared conformance suite run against **both** backends, proving the parity rule the gateway's own `CLAUDE.md` states ("test both stores... nothing forces the two to agree") | 2026-09-03 | [conformance_test.go](../../conformance_test.go) — 8 subtests, run by `TestMemoryRepository` (`ledger_test.go`) and by `TestPostgresRepository` (`postgres_integration_test.go`, `POSTGRES_URL`-gated, skips cleanly when unset — same convention as `taskmanager`'s own integration test). **Run for real this session**, not left as "no database available": against a local dev Postgres (`ndikpiny-postgres-1`, `mwanachama-backend-shared`'s own `postgres.Backend`, scratch `accttest_`-prefixed tables created by `postgres.DDL` and dropped by `t.Cleanup` — verified empty afterwards with `\dt accttest_*`). `go build ./...`, `go vet ./...`, `go test -race -tags=integration ./...`, `gofmt -l .` all clean, both backends agreeing on every assertion. One real bug found and fixed by running against real Postgres rather than only the fake: the table-name prefix carried a hyphen from an agency id and Postgres rejected the DDL outright — fixed by dropping the interposed agency id from the prefix (agency scoping already lives in the `agency_id` column, not the table name), the same lesson `taskmanager`'s W9 notes about the memory/Postgres blind spot the two backends don't automatically share. |

## Archived board context

`mwanachama-backend-accounting` — Go library, module
`github.com/aosanya/mwanachama-backend-accounting`. Will be consumed by
[mwanachama-backend-api-gateway](../../../mwanachama-backend-api-gateway)
once W9 lands.

### Why this repo exists

Mwanachama's only monetary object, `contribution`, is a mirror of an
outside paybill statement — it has never been a ledger the organization
owns. The merchandise domain already proved that a real double-entry
ledger works well in this platform (`internal/domain/merchandise`, ported
2026-08-24 under DEV-1306), but its invariant is held-stock, not zero-sum
currency, so it was not generalized — this is a new, purpose-built general
ledger instead, arrived at through a dev-research session on 2026-09-03
(recorded as [DSN-1698](../../../developer/documentation/2.%20design/todo.md)).

**Decision: new repo, same architecture as `taskmanager`.** A Go package
on `mwanachama-backend-shared`'s Postgres entity-graph store, imported
directly by `mwanachama-backend-api-gateway`. No gRPC, no sub-service
shape, no Supabase, no RLS.
